<% if @product.nil? %>
<h1>Hmm. Didn't find that!</h1>
<% else %>

<%= react_component("product/Product",
  {
    id: @product.id,
    title: @product.title,
    price: @product.price,
    imagePath: product_image_url(@product.id),
    addToCartURL: baskets_add_url,
    basketID: basket_id
  },
  {
    class: "product"
  })
%>
<% end %>
